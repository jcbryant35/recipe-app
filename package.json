{
  "name": "api",
  "version": "1.0.0",
  "description": "Recipe App ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
      "start": "node server.js",
      "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "keywords": [
    "recipes"
  ],
  "author": "Jason C. Bryant",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongodb": "^3.6.3",
    "nodemailer": "^6.5.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": "12.14.1"
  }
}
